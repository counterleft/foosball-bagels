:ruby
  if !@bagels.nil? && !@bagels.empty?
    haml_concat %(<table id="recent_bagels"><tr><th>Baked On</th><th>Owner</th><th>Teammate</th><th>Off. Opponent</th><th>Def. Opponent</th></tr>).html_safe
  elsif
    haml_concat "<p>#{@player.name} has not been involved in any bagels.</p>".html_safe
  end

- (@bagels || []).each do |bagel|
  %tr
    %td= bagel.baked_on_display
    %td= bagel.owner_name
    %td= bagel.teammate_name
    %td= bagel.opponent_1_name
    %td= bagel.opponent_2_name
    %td= link_to "Show", bagel

:ruby
  if !@bagels.nil? && !@bagels.empty?
    haml_concat "</table>".html_safe
  end

= will_paginate @bagels if @bagels
