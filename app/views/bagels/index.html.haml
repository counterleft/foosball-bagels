%h2 Bagels

- if flash[:notice]
  %div.alert.alert-success
    %button.close(data-dismiss="alert") &times;
    %p= flash[:notice]

#bagels
  %table.table.table-condensed.table-striped
    %thead
      %th Baked On
      %th Owner
      %th Teammate
      %th Winning Offensive Player
      %th Winning Defensive Player
    %tbody
      - @bagels.each do |bagel|
        %tr
          %td= bagel.baked_on.strftime("%Y-%m-%d")
          %td= bagel.owner.name || "-"
          %td= bagel.teammate.nil? ? "-" : bagel.teammate.name
          %td= bagel.opponent_1.nil? ? "-" : bagel.opponent_1.name
          %td= bagel.opponent_2.nil? ? "-" : bagel.opponent_2.name
