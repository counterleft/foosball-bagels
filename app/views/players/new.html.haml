#main-box
  #quote Fresh Meat?

#content
  - form_for(@player) do |f|
    = f.error_messages

    %p
      = f.label :name
      %br
      = f.text_field :name

    %p= f.submit 'Create Player'

  = link_to 'Back', root_path